name: Warm Render API

on:
  schedule:
    - cron: "30-59/5 4 * * *"  # 10:00-10:25 IST
    - cron: "*/5 5-13 * * *"   # 10:30-19:25 IST
    - cron: "0 14 * * *"       # 19:30 IST
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit health endpoint
        run: curl -fsS "https://atharva-ai-51gs.onrender.com/health" > /dev/null
